namespace RegOS.ReferenceData.Application.Queries.Organization.ListContactRoles;

/// <summary>
/// "Which roles can a person hold?" — the platform's, plus this tenant's own.
/// </summary>
public sealed record ListContactRolesQuery;
